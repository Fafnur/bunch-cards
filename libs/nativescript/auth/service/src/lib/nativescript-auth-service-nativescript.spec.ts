import { nativescriptAuthServiceNativescript } from './nativescript-auth-service-nativescript';

describe('nativescriptAuthServiceNativescript', () => {
  it('should work', () => {
    expect(nativescriptAuthServiceNativescript()).toEqual('nativescript-auth-service');
  });
});
